# Environment files
.env
.env.local
.env.*.local
.env.backup

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov/

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output/

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt/

# Bower dependency directory (https://bower.io/)
bower_components/

# node_modules (if you add any Node.js dependencies)
node_modules/
*/node_modules/

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env
.env.test
.env.production
.env.staging

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next

# Nuxt.js build / generate output
.nuxt
dist

# Gatsby files
.cache/
public

# Storybook build outputs
.out
.storybook-out

# Temporary folders
tmp/
temp/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Docker volumes (persistent data - be careful with these)
# Uncomment if you want to exclude persistent data from git
# But this might delete your n8n data!
# postgres_data/
# n8n_data/
# redis_data/

# Backup files (uncomment if you don't want to commit backups)
# backups/*.sql
# backups/*.tar.gz

# Caddy data
caddy/data/
caddy/config/

# Shared files that might contain sensitive data
shared/sensitive/
shared/credentials/

# Custom nodes with node_modules
custom-nodes/*/node_modules/

# Local development files
*.local
.local

# SSL certificates (if any)
*.pem
*.key
*.crt
*.cert

# GitHub Integration - SSH Keys (NEVER commit these!)
git-config/.ssh/id_*
git-config/.ssh/known_hosts
git-config/.ssh/config
# But we do want to track the .ssh directory itself
!git-config/.ssh/.gitkeep

# Git repositories (these should be managed separately)
git-repos/

# Database dumps
*.dump
*.sql.gz
